import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class AppColors {
  // Gradient colors for full-page background
  static const Color gradientStart = Color(0xFF1A1B2E); // Dark blue
  static const Color gradientEnd = Color(0xFF2E2F4A);   // Lighter blue

  // Primary and secondary colors
  static const Color primary = Color(0xFF6B48FF); // Vibrant purple
  static const Color secondary = Color(0xFF00DDEB); // Cyan accent

  // Text field and card colors
  static const Color textFieldFill = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFF25273D); // current dark bluish-gray
 // Card background

  // Text colors
  static const Color textPrimary = Color(0xFF212121); // A dark grey (not pure black)
  static const Color textSecondary = Color(0xFF424242); // Slightly lighter than primary
  static const Color textHighlight = Color(0xFF00DDEB); // Short URL and cursor

  // Button and feedback colors
  static const Color buttonShadow = Color(0xFF6B48FF);
  static const textfieldborder= Color(0xffD0D0D0);
  static const brandNew = Color(0xFF6E5FDE);
  static const mobilenumber=Color(0xff1e232c);
  static const greyText = Color(0xFF667085);

}